version: '3.8'

services:
  # ===============================
  # NGINX PROXY MANAGER
  # ===============================
  nginxproxymanager:
    image: jc21/nginx-proxy-manager:latest
    container_name: nginx-proxy-manager
    restart: unless-stopped
    ports:
      - "80:80"       # HTTP
      - "443:443"     # HTTPS
      - "81:81"       # UI dashboard
    volumes:
      - npm_data:/data
      - npm_letsencrypt:/etc/letsencrypt

  # ===============================
  # BACKEND (Spring Boot)
  # ===============================
  backend:
    build: ./backend
    container_name: backend
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    expose:
      - "8080"

  # ===============================
  # FRONTEND (React / Vue / Angular)
  # chạy build static bằng nginx
  # ===============================
  frontend:
    build: ./frontend
    container_name: frontend
    restart: always
    expose:
      - "80"

volumes:
  npm_data:
  npm_letsencrypt:
